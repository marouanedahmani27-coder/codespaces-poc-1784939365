{
  "name": "codespaces-research",
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu",
  "features": {
    "ghcr.io/devcontainers/features/sshd:1": {
      "version": "latest"
    }
  },
  "postCreateCommand": "echo PC_MARKER=$(date +%s)_$(hostname)_$(id -u) > /tmp/postcreate_marker.txt && env | sort > /tmp/env_snapshot.txt && echo done-postCreate",
  "onCreateCommand": "echo ONC_MARKER=$(date +%s) > /tmp/oncreate_marker.txt"
}
